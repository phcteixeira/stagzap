version: '3.8'

services:
  postgres_disparador:
    image: postgres:16-alpine
    environment:
      - POSTGRES_DB=contacts
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=Exclusivo2025
    volumes:
      - postgres_disparador_data:/var/lib/postgresql/data
    networks:
      - stagnet
    ports:
      - 1914:5432
    deploy:
      placement:
        constraints:
          - node.role == manager
      restart_policy:
        condition: on-failure

  redis_disparador:
    image: redis:7-alpine
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_disparador_data:/data
    networks:
      - stagnet
    deploy:
      placement:
        constraints:
          - node.role == manager
      restart_policy:
        condition: on-failure

  backend:
    image: astraonline/astracampaignbackend:0.0.3
    environment:
      DATABASE_URL: postgresql://postgres:Exclusivo2025@postgres_disparador:5432/contacts?schema=public
      REDIS_URL: redis://redis_disparador:6379
      REDIS_PREFIX: work_app
      PORT: 3001
      NODE_ENV: production
      JWT_SECRET: your-super-secret-jwt-key-at-least-32-characters-long-trecofantastico
      JWT_EXPIRES_IN: 24h
      DEFAULT_WAHA_HOST: ""
      DEFAULT_WAHA_API_KEY: ""
      DEFAULT_EVOLUTION_HOST: ""
      DEFAULT_EVOLUTION_API_KEY: ""
      DEFAULT_COMPANY_NAME: ""
      DEFAULT_PAGE_TITLE: "Sistema de Gestão de Contatos"
      ALLOWED_ORIGINS: https://stag-dev.caradotrafego.com.br,http://stag-dev.caradotrafego.com.br,http://work_frontend,http://work_frontend:80
    networks:
      - stagnet
    depends_on:
      - postgres_disparador
      - redis_disparador
    volumes:
      - contacts_data:/app/data
      - uploads_data:/app/uploads
      - backup_data:/app/backups
    ports:
      - 3005:3006
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      restart_policy:
        condition: on-failure
      labels:
        - traefik.enable=true
        - traefik.docker.network=stagnet
        - traefik.http.routers.work-backend.rule=Host(`stag-dev.caradotrafego.com.br`) && PathPrefix(`/api`)
        - traefik.http.routers.work-backend.entrypoints=web,websecure
        - traefik.http.routers.work-backend.tls=true
        - traefik.http.routers.work-backend.tls.certresolver=letsencryptresolver
        - traefik.http.services.work-backend.loadbalancer.server.port=3001

  frontend:
    image: astraonline/astracampaignfrontend:0.0.4
    networks:
      - stagnet
    depends_on:
      - backend
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.role == manager
      restart_policy:
        condition: on-failure
      labels:
        - traefik.enable=true
        - traefik.docker.network=stagnet
        - traefik.http.routers.work-frontend.rule=Host(`stag-dev.caradotrafego.com.br`)
        - traefik.http.routers.work-frontend.entrypoints=web,websecure
        - traefik.http.routers.work-frontend.tls=true
        - traefik.http.routers.work-frontend.tls.certresolver=letsencryptresolver
        - traefik.http.services.work-frontend.loadbalancer.server.port=80

networks:
  stagnet:
    external: true

volumes:
  postgres_disparador_data:
  redis_disparador_data:
  contacts_data:
  uploads_data:
  backup_data:
